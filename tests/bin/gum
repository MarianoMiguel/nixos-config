#!/usr/bin/env bash
set -euo pipefail

command_name=${1:-}
shift || true

case $command_name in
  style)
    printf '%s\n' "$*"
    ;;
  *)
    printf 'Unexpected gum command in preview test: %s\n' "$command_name" >&2
    exit 1
    ;;
esac
