monitor.bluez.properties = {
  # Let AirPods stem presses control MPRIS-aware media players.
  # Do not run mpris-proxy alongside this; the two AVRCP players conflict.
  bluez5.dummy-avrcp-player = true
}
